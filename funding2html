<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1.0" />
  <title>2025-12-25 — KETADATA // FUNDING TERMINAL</title>
  <style>
    :root{
      --bg:#000;
      --fg:#fff;
      --ui: Arial, Helvetica, sans-serif;

      --topH: 38px;
      --botH: 26px;

      --hair: rgba(255,255,255,.14);
      --hair2: rgba(255,255,255,.08);

      /* Intensifiers (0..1) */
      --I_VOL: 0.84;     /* volatility of bar + number smear */
      --I_BLUR: 0.78;    /* goal entropy blur */
      --I_GLITCH: 0.82;  /* cut-up jitter / tears */
      --I_SEAR: 0.70;    /* glow / burn */
      --I_NOISE: 0.68;   /* background signal */
    }

    *{ box-sizing:border-box; }
    html,body{ height:100%; }
    body{
      margin:0;
      background:var(--bg);
      color:var(--fg);
      font-family:var(--ui);
      overflow:hidden;
    }

    /* =========================================================
       BACKGROUND FIELD
    ========================================================= */
    #field{
      position:fixed; inset:0; z-index:0;
      background:
        radial-gradient(1200px 700px at 50% 20%, rgba(255,255,255,.06), transparent 70%),
        radial-gradient(1000px 680px at 20% 78%, rgba(255,255,255,.04), transparent 72%),
        radial-gradient(900px 520px at 82% 82%, rgba(255,255,255,.04), transparent 72%),
        linear-gradient(180deg, #000, #020202 55%, #000);
    }

    /* wire grid */
    #field::before{
      content:"";
      position:absolute; inset:0;
      background:
        linear-gradient(rgba(255,255,255,.035) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255,255,255,.035) 1px, transparent 1px);
      background-size: 52px 52px;
      opacity:.62;
      mix-blend-mode: overlay;
      pointer-events:none;
      filter: blur(0.25px);
    }

    /* noise layer */
    #noise{
      position:fixed; inset:0; z-index:1;
      pointer-events:none;
      opacity: calc(var(--I_NOISE) * .75);
      mix-blend-mode: overlay;
      background-image:
        repeating-linear-gradient(0deg, rgba(255,255,255,.05), rgba(255,255,255,.05) 1px, transparent 1px, transparent 3px);
      filter: blur(0.6px);
      transform: translateZ(0);
    }
    #noise::after{
      content:"";
      position:absolute; inset:-30%;
      background:
        radial-gradient(closest-side, rgba(255,255,255,.08), transparent 70%),
        conic-gradient(from 10deg, rgba(255,255,255,.06), transparent, rgba(255,255,255,.05), transparent);
      opacity:.35;
      animation: drift 5.5s linear infinite;
      filter: blur(10px);
      transform: translateZ(0);
    }
    @keyframes drift{
      from{ transform: translate(-6%, -2%) rotate(0deg); }
      to{ transform: translate(6%, 2%) rotate(360deg); }
    }

    /* =========================================================
       LIGHTS BEHIND (1–6)
    ========================================================= */
    #lights{
      position:fixed; inset:0; z-index:2;
      pointer-events:none;
      opacity:0;
      mix-blend-mode: screen;
    }
    #lights.on{ opacity:1; }
    #lights::before{
      content:"";
      position:absolute; inset:-30%;
      background:
        radial-gradient(closest-side at 50% 50%, rgba(255,255,255,.25), transparent 72%),
        conic-gradient(from 0deg, rgba(255,255,255,.10), transparent, rgba(255,255,255,.08), transparent);
      opacity: .25;
      filter: blur(12px);
      transform: rotate(12deg);
    }
    @keyframes strobe1 { 0%,49%{opacity:0} 50%,100%{opacity:1} }
    @keyframes strobe2 { 0%,24%{opacity:0} 25%,49%{opacity:1} 50%,74%{opacity:0} 75%,100%{opacity:1} }
    @keyframes strobe3 { 0%,14%{opacity:0} 15%,29%{opacity:1} 30%,44%{opacity:0} 45%,59%{opacity:1} 60%,74%{opacity:0} 75%,100%{opacity:1} }
    @keyframes strobe4 { 0%,9%{opacity:0} 10%,19%{opacity:1} 20%,29%{opacity:0} 30%,39%{opacity:1} 40%,49%{opacity:0} 50%,59%{opacity:1} 60%,69%{opacity:0} 70%,79%{opacity:1} 80%,89%{opacity:0} 90%,100%{opacity:1} }
    @keyframes strobe5 { 0%,7%{opacity:0} 8%,15%{opacity:1} 16%,23%{opacity:0} 24%,31%{opacity:1} 32%,39%{opacity:0} 40%,47%{opacity:1} 48%,55%{opacity:0} 56%,63%{opacity:1} 64%,71%{opacity:0} 72%,79%{opacity:1} 80%,87%{opacity:0} 88%,95%{opacity:1} 96%,100%{opacity:0} }
    @keyframes strobe6 { 0%,3%{opacity:0} 4%,7%{opacity:1} 8%,11%{opacity:0} 12%,15%{opacity:1} 16%,19%{opacity:0} 20%,23%{opacity:1} 24%,27%{opacity:0} 28%,31%{opacity:1} 32%,35%{opacity:0} 36%,39%{opacity:1} 40%,43%{opacity:0} 44%,47%{opacity:1} 48%,51%{opacity:0} 52%,55%{opacity:1} 56%,59%{opacity:0} 60%,63%{opacity:1} 64%,67%{opacity:0} 68%,71%{opacity:1} 72%,75%{opacity:0} 76%,79%{opacity:1} 80%,83%{opacity:0} 84%,87%{opacity:1} 88%,91%{opacity:0} 92%,95%{opacity:1} 96%,100%{opacity:0} }

    /* =========================================================
       CHROME (TOP/BOTTOM) — fix: no overlap, hard docking
    ========================================================= */
    .topbar{
      position:fixed; left:0; right:0; top:0; height:var(--topH);
      z-index:60;
      display:flex; align-items:center; justify-content:space-between;
      padding:0 12px;
      border-bottom:1px solid var(--hair);
      background: rgba(0,0,0,.38);
      backdrop-filter: blur(8px);
      user-select:none;
    }
    .brandMini{
      display:flex; align-items:center; gap:10px;
      font-size:11px; letter-spacing:.18em; text-transform:uppercase;
      color: rgba(255,255,255,.85);
      min-width:0;
    }
    .brandMini span:last-child{
      white-space:nowrap; overflow:hidden; text-overflow:ellipsis;
      max-width: 58vw;
    }
    .topRight{
      display:flex; align-items:center; justify-content:flex-end;
      gap:8px;
      flex: 0 0 auto;
      min-width: 0;
    }
    .btn{
      font-family:var(--ui);
      font-size:11px; letter-spacing:.18em; text-transform:uppercase;
      color: rgba(255,255,255,.82);
      background: rgba(0,0,0,.30);
      border:1px solid var(--hair);
      padding:6px 10px;
      cursor:pointer;
      line-height: 1;
      height: 28px;
      display:flex;
      align-items:center;
    }
    .btn:hover{ border-color: rgba(255,255,255,.30); color:#fff; }
    .btn:active{ transform: translateY(1px); }

    /* Global KETA_NOTE icon law */
    .noteIcon{
      width:12px; height:12px;
      display:inline-block;
      border:1px solid #fff;
      background:#fff; /* collapsed */
      cursor:pointer;
      flex:0 0 auto;
      margin-left: 2px;
      margin-right: 2px;
    }
    .noteIcon.outlined{ background: transparent; } /* open */

    .footer{
      position:fixed; left:0; right:0; bottom:0; height:var(--botH);
      z-index:60;
      display:flex; align-items:center; justify-content:space-between;
      padding:0 10px;
      border-top:1px solid var(--hair);
      background: rgba(0,0,0,.38);
      backdrop-filter: blur(8px);
      font-size:10px; letter-spacing:.14em; text-transform:uppercase;
      color: rgba(255,255,255,.70);
      user-select:none;
    }
    .footer b{ color:#fff; font-weight:900; }
    .muted{ color: rgba(255,255,255,.55); }

    /* =========================================================
       STAGE — less buttoned-up, big center numbers
    ========================================================= */
    #stage{
      position:fixed; inset:0;
      z-index:10;
      padding-top: var(--topH);
      padding-bottom: var(--botH);
      overflow:hidden;
    }

    /* center pressure field */
    #center{
      position:absolute; inset:0;
      display:flex;
      align-items:center;
      justify-content:center;
      padding: 16px;
      pointer-events:none; /* clicks pass through unless panel */
    }

    /* Big goal stack */
    #goalStack{
      position:relative;
      width: min(1200px, 94vw);
      height: min(520px, 58vh);
      pointer-events:none;
      transform: translateZ(0);
    }

    /* “cut-up” slices overlaying the goal */
    .slice{
      position:absolute;
      left: 50%;
      transform: translateX(-50%);
      width: min(1200px, 94vw);
      height: 120px;
      overflow:hidden;
      pointer-events:none;
      mix-blend-mode: screen;
      opacity: calc(.25 + var(--I_GLITCH) * .45);
      border-top: 1px solid rgba(255,255,255,.06);
      border-bottom: 1px solid rgba(255,255,255,.06);
      background: rgba(0,0,0,.12);
      backdrop-filter: blur(2px);
    }
    .slice .inner{
      position:absolute; left:0; top:0;
      width:100%; height:100%;
      display:flex; align-items:center; justify-content:center;
      will-change: transform, filter;
    }

    /* main goal */
    #goalNum{
      position:absolute; inset:0;
      display:flex;
      align-items:center;
      justify-content:center;
      text-align:center;
      font-weight: 900;
      letter-spacing: .08em;
      text-transform: uppercase;
      color: #fff;
      filter: blur(calc(var(--I_BLUR) * 5.5px));
      text-shadow:
        0 0 calc(var(--I_SEAR) * 40px) rgba(255,255,255,.75),
        0 0 calc(var(--I_SEAR) * 110px) rgba(255,255,255,.28);
      opacity: .98;
      transform: translateZ(0);
    }

    /* responsive goal size */
    #goalNum .big{
      font-size: clamp(54px, 7.8vw, 128px);
      line-height: 1.0;
      white-space: nowrap;
    }
    #goalNum .sub{
      margin-top: 12px;
      font-size: 11px;
      letter-spacing: .22em;
      opacity: .70;
      white-space: nowrap;
    }

    /* a more violent ghost smear */
    #goalNum::before,
    #goalNum::after{
      content: attr(data-goal);
      position:absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      font-weight: 900;
      letter-spacing: .08em;
      font-size: clamp(54px, 7.8vw, 128px);
      white-space: nowrap;
      opacity: .55;
      filter: blur(calc(var(--I_BLUR) * 9px));
      mix-blend-mode: screen;
      pointer-events:none;
    }
    #goalNum::before{
      transform: translate(calc(-50% + var(--gdx) * 1px), calc(-50% + var(--gdy) * 1px));
      opacity: .62;
    }
    #goalNum::after{
      transform: translate(calc(-50% + var(--gdx2) * 1px), calc(-50% + var(--gdy2) * 1px));
      opacity: .35;
    }

    /* volatile bar — centered, tearing */
    #barBlock{
      position:absolute;
      left: 50%;
      bottom: 12px;
      transform: translateX(-50%);
      width: min(1200px, 94vw);
      pointer-events:none;
    }
    #barOuter{
      height: 28px;
      border:1px solid rgba(255,255,255,.16);
      background: rgba(0,0,0,.22);
      position:relative;
      overflow:hidden;
      box-shadow: 0 20px 90px rgba(0,0,0,.60);
    }
    #barFill{
      position:absolute; left:0; top:0; bottom:0;
      width: 33%;
      background: rgba(255,255,255,.94);
      opacity: .90;
      filter: blur(calc(var(--I_SEAR) * 2.5px));
      transform-origin: left center;
      will-change: transform, width;
    }
    /* tear masks */
    .tear{
      position:absolute; top:-60%;
      width: 180px; height: 220%;
      background:
        linear-gradient(90deg, transparent, rgba(0,0,0,.55), transparent),
        radial-gradient(closest-side, rgba(0,0,0,.35), transparent 70%);
      mix-blend-mode: multiply;
      opacity: calc(.12 + var(--I_GLITCH) * .55);
      filter: blur(0.6px);
      will-change: transform;
    }

    /* bar scanline */
    #barOuter::after{
      content:"";
      position:absolute; inset:0;
      background:
        linear-gradient(90deg, rgba(0,0,0,0), rgba(0,0,0,.22), rgba(0,0,0,0));
      opacity: calc(var(--I_GLITCH) * .70);
      animation: scanx 0.9s linear infinite;
      mix-blend-mode: multiply;
    }
    @keyframes scanx{
      from{ transform: translateX(-110%); }
      to{ transform: translateX(110%); }
    }

    #barMeta{
      margin-top: 10px;
      display:flex;
      justify-content:space-between;
      gap: 12px;
      flex-wrap:wrap;
      font-size:10px;
      letter-spacing:.18em;
      text-transform:uppercase;
      color: rgba(255,255,255,.70);
    }
    #barMeta b{ color:#fff; font-weight:900; }

    /* =========================================================
       LOWER CONTENT — still present, but less “office”
    ========================================================= */
    #lower{
      position:absolute;
      left: 16px;
      right: 16px;
      bottom: 14px;
      pointer-events:auto;
      display:grid;
      grid-template-columns: 1.1fr .9fr;
      gap: 12px;
      opacity: .92;
      transform: translateZ(0);
    }
    .panel{
      border:1px solid rgba(255,255,255,.14);
      background: rgba(0,0,0,.28);
      backdrop-filter: blur(8px);
      box-shadow: 0 22px 90px rgba(0,0,0,.58);
    }
    .panelHead{
      height: 34px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      padding: 0 10px;
      border-bottom: 1px solid rgba(255,255,255,.08);
      user-select:none;
    }
    .panelHead .label{
      font-size:10px;
      letter-spacing:.20em;
      text-transform:uppercase;
      font-weight:900;
      color: rgba(255,255,255,.80);
    }
    .miniBtn{
      width: 26px;
      height: 22px;
      display:flex;
      align-items:center;
      justify-content:center;
      border:1px solid rgba(255,255,255,.14);
      background: rgba(0,0,0,.22);
      color: rgba(255,255,255,.85);
      cursor:pointer;
      font-weight:900;
      user-select:none;
    }
    .miniBtn:hover{ border-color: rgba(255,255,255,.28); color:#fff; }

    .panelBody{ padding: 10px; }
    .copy{
      font-size:12px;
      letter-spacing:.06em;
      line-height:1.45;
      color: rgba(255,255,255,.76);
    }
    .copy b{ color:#fff; font-weight:900; }
    .mono{
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
      font-size: 11px;
      letter-spacing:.06em;
      color: rgba(255,255,255,.76);
      word-break: break-all;
    }
    .tagInput{
      width:100%;
      border:1px solid rgba(255,255,255,.14);
      background: rgba(0,0,0,.40);
      color:#fff;
      padding: 10px;
      outline:none;
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
      font-size: 11px;
      letter-spacing:.08em;
    }
    .tagInput::placeholder{ color: rgba(255,255,255,.38); }

    .payGrid{
      display:grid;
      grid-template-columns: 1fr auto;
      gap: 8px 10px;
      align-items:center;
    }
    .lbl{
      font-size:10px;
      letter-spacing:.18em;
      text-transform:uppercase;
      color: rgba(255,255,255,.62);
      margin-top: 8px;
      margin-bottom: 6px;
    }

    /* ledger */
    #ledgerWrap{
      grid-column: 1 / span 2;
      max-height: 26vh;
      overflow:auto;
    }
    #ledgerWrap::-webkit-scrollbar{ height:10px; width:10px; }
    #ledgerWrap::-webkit-scrollbar-thumb{ background: rgba(255,255,255,.12); }
    table{
      width:100%;
      border-collapse:collapse;
      font-size: 11px;
      letter-spacing:.08em;
    }
    th, td{
      padding: 10px 8px;
      border-bottom:1px solid rgba(255,255,255,.08);
      color: rgba(255,255,255,.74);
      text-align:left;
      vertical-align:top;
    }
    th{
      color: rgba(255,255,255,.90);
      font-size: 10px;
      letter-spacing:.20em;
      text-transform:uppercase;
      font-weight:900;
      position:sticky;
      top:0;
      background: rgba(0,0,0,.60);
      backdrop-filter: blur(8px);
      border-bottom:1px solid rgba(255,255,255,.14);
      z-index:2;
    }
    .etch{ color:#fff; font-weight:900; }
    .pill{
      display:inline-block;
      border:1px solid rgba(255,255,255,.14);
      padding: 4px 8px;
      background: rgba(0,0,0,.25);
      color: rgba(255,255,255,.78);
      font-size:10px;
      letter-spacing:.14em;
      text-transform:uppercase;
      user-select:none;
      white-space:nowrap;
    }

    /* =========================================================
       KETA_NOTE (GLOBAL) — draggable, icon fill law
    ========================================================= */
    #note{
      position:fixed;
      top: 54px;
      left: 14px;
      z-index:70;
      width: min(540px, 88vw);
      height: 240px;
      border:1px solid rgba(255,255,255,.14);
      background: rgba(0,0,0,.34);
      backdrop-filter: blur(10px);
      display:none;
      box-shadow: 0 22px 90px rgba(0,0,0,.60);
    }
    #note.open{ display:block; }
    #noteHead{
      height: 34px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      padding: 0 10px;
      border-bottom:1px solid rgba(255,255,255,.08);
      cursor:move;
      user-select:none;
    }
    #noteHead .label{
      font-size:11px;
      letter-spacing:.22em;
      text-transform:uppercase;
      font-weight:900;
      color: rgba(255,255,255,.86);
    }
    #noteArea{
      width:100%;
      height: calc(100% - 34px);
      border:0;
      resize:none;
      outline:none;
      padding: 10px 10px;
      background: transparent;
      color:#fff;
      font-family: var(--ui);
      font-size: 12px;
      letter-spacing:.06em;
      line-height:1.45;
    }

    /* =========================================================
       INTENSIFIERS — FIX: docked under topbar, never overlaps bar
       (can be hidden; draggable if you want)
    ========================================================= */
    #ctl{
      position:fixed;
      right: 14px;
      top: calc(var(--topH) + 10px);
      z-index:65;
      width: min(360px, 44vw);
      border:1px solid rgba(255,255,255,.14);
      background: rgba(0,0,0,.32);
      backdrop-filter: blur(10px);
      box-shadow: 0 22px 90px rgba(0,0,0,.60);
    }
    #ctl.hidden{ display:none; }
    #ctlHead{
      height:34px;
      display:flex; align-items:center; justify-content:space-between;
      padding:0 10px;
      border-bottom:1px solid rgba(255,255,255,.08);
      cursor:move;
      user-select:none;
    }
    #ctlHead .label{
      font-size:11px;
      letter-spacing:.22em;
      text-transform:uppercase;
      font-weight:900;
      color: rgba(255,255,255,.86);
    }
    #ctlBody{
      padding:10px;
      display:grid;
      grid-template-columns: 1fr 1fr;
      gap:10px;
    }
    .knob{
      border:1px solid rgba(255,255,255,.08);
      padding:10px;
      background: rgba(0,0,0,.18);
    }
    .knob .k{
      font-size:10px;
      letter-spacing:.14em;
      text-transform:uppercase;
      color: rgba(255,255,255,.78);
      display:flex;
      justify-content:space-between;
      gap:10px;
      margin-bottom:8px;
      white-space:nowrap;
      overflow:hidden;
      text-overflow:ellipsis;
    }
    .knob input[type="range"]{
      width:100%;
      accent-color: #fff;
      background: transparent;
    }
    .knob .v{
      font-weight:900;
      color:#fff;
      letter-spacing:.08em;
    }

    /* =========================================================
       STATE IO — minimal panel
    ========================================================= */
    #statePanel{
      position:fixed;
      right:14px;
      bottom: calc(var(--botH) + 12px);
      z-index:70;
      width:min(560px, 56vw);
      border:1px solid rgba(255,255,255,.14);
      background:rgba(0,0,0,.34);
      backdrop-filter: blur(10px);
      padding:10px;
      display:none;
      box-shadow: 0 22px 90px rgba(0,0,0,.60);
    }
    #statePanel .head{
      display:flex;
      align-items:center;
      justify-content:space-between;
      margin-bottom:8px;
      user-select:none;
    }
    #statePanel .head .label{
      font-size:11px;
      letter-spacing:.22em;
      text-transform:uppercase;
      font-weight:900;
      color: rgba(255,255,255,.86);
    }
    #stateText{
      width:100%;
      height:180px;
      resize:vertical;
      border:1px solid rgba(255,255,255,.12);
      background:rgba(0,0,0,.40);
      color:#fff;
      padding:10px;
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
      font-size:11px;
      letter-spacing:.06em;
      line-height:1.35;
      outline:none;
    }
    #statePanel .row{
      display:flex;
      gap:10px;
      margin-top:8px;
      flex-wrap:wrap;
    }

    /* =========================================================
       NULL MODE LAW: hide chrome/controllers/note only
    ========================================================= */
    body.null .topbar,
    body.null .footer,
    body.null #note,
    body.null #ctl,
    body.null #statePanel,
    body.null #lower { display:none !important; }

    body.invert{ filter: invert(1) hue-rotate(180deg); }

    /* =========================================================
       CUT-UP JITTER (global variables set by JS)
    ========================================================= */
    #stage{
      transform: translate(calc(var(--jx,0) * 1px), calc(var(--jy,0) * 1px));
    }
  </style>
</head>

<body>
  <div id="field" aria-hidden="true"></div>
  <div id="noise" aria-hidden="true"></div>
  <div id="lights" aria-hidden="true"></div>

  <div class="topbar">
    <div class="brandMini">
      <span style="display:inline-block;width:10px;height:10px;border:1px solid #fff;"></span>
      <span>2025-12-25 // FUNDING TERMINAL</span>
    </div>
    <div class="topRight">
      <button class="btn" id="btnState">STATE</button>
      <span class="noteIcon" id="ketaNoteIcon" title="KETA_NOTE" aria-label="KETA_NOTE"></span>
      <button class="btn" id="btnCtl">INTENSIFIERS</button>
      <button class="btn" id="goHome">HOME</button>
      <button class="btn" id="goLand">LAND</button>
    </div>
  </div>

  <div id="stage">
    <!-- BIG CENTER -->
    <div id="center">
      <div id="goalStack">
        <div id="goalNum" data-goal="$12,000,000">
          <div>
            <div class="big" id="goalText">$12,000,000</div>
            <div class="sub">GOAL IS SEVEN+ DIGITS · NUMBER REFUSES STABILITY · BAR IS VOLATILITY</div>
          </div>
        </div>

        <!-- cut-up slices -->
        <div class="slice" style="top: 14%;"><div class="inner" id="s1"></div></div>
        <div class="slice" style="top: 36%;"><div class="inner" id="s2"></div></div>
        <div class="slice" style="top: 58%;"><div class="inner" id="s3"></div></div>

        <!-- bar -->
        <div id="barBlock">
          <div id="barOuter">
            <div id="barFill"></div>
            <div class="tear" id="t1"></div>
            <div class="tear" id="t2"></div>
            <div class="tear" id="t3"></div>
          </div>
          <div id="barMeta">
            <div>READOUT: <b id="pct">—</b></div>
            <div>VOLATILITY: <b id="vol">—</b></div>
            <div>STATE: <b id="stateSig">LIVE</b></div>
            <div class="muted">SIGNAL ≠ COMPLETION</div>
          </div>
        </div>
      </div>
    </div>

    <!-- LOWER CONTENT -->
    <div id="lower">
      <div class="panel">
        <div class="panelHead">
          <div class="label">STATEMENT</div>
          <div style="display:flex;gap:8px;">
            <div class="miniBtn" id="stmtCollapse" title="collapse">–</div>
          </div>
        </div>
        <div class="panelBody" id="stmtBody">
          <div class="copy">
            KETADATA IS A MULTIMODAL OPERATING SYSTEM FOR ARCHIVE, RITUAL, MEDIA, AND SIGNAL.
            IT IS A <b>STAGE</b> FOR INTERFACES: LIGHTS, INTENSITIES, PERFORMANCE CONTROL — WITH PORTABLE STATE.
            THIS PAGE IS AN <b>OPEN CHANNEL</b>. CONTRIBUTIONS BUY TIME, BANDWIDTH, AND HARDENED CONTINUITY.
          </div>
          <div class="copy" style="margin-top:10px;">
            BY CONTRIBUTING YOU MAY <b>ETCH YOUR TAG</b> INTO THE LEDGER AS TRACE.
            (FRONT-END DISPLAY. CONNECT PAYMENT BACKEND LATER.)
          </div>
        </div>
      </div>

      <div class="panel">
        <div class="panelHead">
          <div class="label">PAY + ETCH TAG</div>
          <div style="display:flex;gap:8px;">
            <div class="miniBtn" id="payCollapse" title="collapse">–</div>
          </div>
        </div>
        <div class="panelBody" id="payBody">
          <input id="tag" class="tagInput" placeholder="YOUR TAG (E.G. 0101KNG1998)" />

          <div class="lbl">PAYMENT METHODS</div>
          <div class="payGrid">
            <div class="mono" id="btcAddr">bc1qxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx</div>
            <button class="btn" id="copyBTC">COPY</button>

            <div class="mono" id="ethAddr">0x0000000000000000000000000000000000000000</div>
            <button class="btn" id="copyETH">COPY</button>

            <div class="mono" id="otherPay">ADD LINK(S) HERE</div>
            <button class="btn" id="copyOther">COPY</button>
          </div>

          <div style="display:flex;gap:10px;margin-top:10px;flex-wrap:wrap;">
            <button class="btn" id="etch">ETCH TAG (LOCAL)</button>
          </div>

          <div class="lbl" style="margin-top:10px;">NOTE</div>
          <div class="copy" style="font-size:11px;opacity:.74;">
            “ETCH” WRITES TO LOCAL STORAGE FOR NOW. IN PRODUCTION, WRITE LEDGER ROWS FROM YOUR BACKEND AFTER PAYMENT CONFIRMATION.
          </div>
        </div>
      </div>

      <div class="panel" id="ledgerWrap">
        <div class="panelHead">
          <div class="label">CONTRIBUTIONS LEDGER</div>
          <div style="display:flex;gap:8px;">
            <div class="miniBtn" id="ledgerCollapse" title="collapse">–</div>
          </div>
        </div>
        <div class="panelBody" id="ledgerBody" style="padding:0;">
          <div style="max-height: 22vh; overflow:auto;" id="ledgerScroll">
            <table>
              <thead>
                <tr>
                  <th style="width:170px;">TIMESTAMP</th>
                  <th style="width:220px;">TAG (ETCH)</th>
                  <th style="width:140px;">METHOD</th>
                  <th>NOTE</th>
                </tr>
              </thead>
              <tbody id="rows"></tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- KETA_NOTE -->
  <div id="note" aria-label="KETA_NOTE">
    <div id="noteHead">
      <div class="label">KETA_NOTE</div>
      <div class="miniBtn" id="noteCollapse" title="collapse">–</div>
    </div>
    <textarea id="noteArea" placeholder="KETA_NOTE (GLOBAL)"></textarea>
  </div>

  <!-- Intensifiers -->
  <div id="ctl">
    <div id="ctlHead">
      <div class="label">INTENSIFIERS</div>
      <div class="miniBtn" id="ctlCollapse" title="collapse">–</div>
    </div>
    <div id="ctlBody">
      <div class="knob">
        <div class="k"><span>VOLATILITY</span><span class="v" id="vVOL"></span></div>
        <input id="rVOL" type="range" min="0" max="1000" value="840" />
      </div>
      <div class="knob">
        <div class="k"><span>BLUR</span><span class="v" id="vBLUR"></span></div>
        <input id="rBLUR" type="range" min="0" max="1000" value="780" />
      </div>
      <div class="knob">
        <div class="k"><span>GLITCH</span><span class="v" id="vGLITCH"></span></div>
        <input id="rGLITCH" type="range" min="0" max="1000" value="820" />
      </div>
      <div class="knob">
        <div class="k"><span>SEAR</span><span class="v" id="vSEAR"></span></div>
        <input id="rSEAR" type="range" min="0" max="1000" value="700" />
      </div>
      <div class="knob" style="grid-column:1 / span 2;">
        <div class="k"><span>NOISE</span><span class="v" id="vNOISE"></span></div>
        <input id="rNOISE" type="range" min="0" max="1000" value="680" />
      </div>
    </div>
  </div>

  <!-- STATE PANEL -->
  <div id="statePanel">
    <div class="head">
      <div class="label">STATE IO</div>
      <button class="btn" id="stateClose">CLOSE</button>
    </div>
    <textarea id="stateText" spellcheck="false"></textarea>
    <div class="row">
      <button class="btn" id="stateExport">EXPORT</button>
      <button class="btn" id="stateCopy">COPY</button>
      <button class="btn" id="stateImport">IMPORT</button>
      <button class="btn" id="stateSave">SAVE</button>
      <button class="btn" id="stateLoad">LOAD</button>
      <button class="btn" id="stateClearLedger">CLEAR LEDGER (LOCAL)</button>
    </div>
  </div>

  <div class="footer">
    <div>
      <span class="muted">NULL:</span> <b id="nullStatus">OFF</b> ·
      <span class="muted">INVERT:</span> <b id="invStatus">OFF</b> ·
      <span class="muted">LIGHT:</span> <b id="lightStatus">0</b>
    </div>
    <div>
      <span class="muted">LEDGER:</span> <b id="ledgerCount">0</b> ·
      <span class="muted">MODE:</span> <b>VOLATILE</b>
    </div>
  </div>

  <script>
    /* =========================================================
       CONTINUITY LINKS
    ========================================================= */
    const SHELL_HOME_URL = "system.html";
    const LANDING_URL = "index11.html";
    document.getElementById('goHome').onclick = ()=> location.href = SHELL_HOME_URL;
    document.getElementById('goLand').onclick = ()=> location.href = LANDING_URL;

    /* =========================================================
       NAMESPACE
    ========================================================= */
    const FILE_ID = "KETADATA_FUNDING_TERMINAL";
    const ROOM_ID = "STORE";
    const VERSION = "v1";
    const STATE_KEY = `KETADATA_STATE::${FILE_ID}::${ROOM_ID}::${VERSION}`;
    const LEDGER_KEY = `KETADATA_LEDGER::${FILE_ID}::${ROOM_ID}::${VERSION}`;

    /* =========================================================
       GLOBAL KETA_NOTE SYNC (anchor)
    ========================================================= */
    const NOTE_KEYS = [
      "KETADATA_NOTE::GLOBAL::v1",
      "KETADATA_GLOBAL_KETA_NOTE_V1",
      "KETADATA_GLOBAL_KETA_NOTE",
      "KETA_NOTE",
      "SHELL8_KETA_NOTE",
      "KETADATA_SHELL8_KETA_NOTE"
    ];
    function readGlobalNote(){
      for(const k of NOTE_KEYS){
        const v = localStorage.getItem(k);
        if(v && v.trim().length) return v;
      }
      return "";
    }
    function writeGlobalNote(v){
      for(const k of NOTE_KEYS) localStorage.setItem(k, v);
    }

    /* =========================================================
       KETA_NOTE ICON LAW
    ========================================================= */
    const note = document.getElementById('note');
    const noteArea = document.getElementById('noteArea');
    const noteIcon = document.getElementById('ketaNoteIcon');
    const noteCollapse = document.getElementById('noteCollapse');
    function setNoteOpen(open){
      note.classList.toggle('open', open);
      noteIcon.classList.toggle('outlined', open); // open -> outline; collapsed -> filled
    }
    setNoteOpen(false);
    noteArea.value = readGlobalNote();
    let noteSaveT=null;
    noteArea.addEventListener('input', ()=>{
      clearTimeout(noteSaveT);
      noteSaveT = setTimeout(()=> writeGlobalNote(noteArea.value), 140);
    });
    noteIcon.addEventListener('click', ()=>{
      const open = !note.classList.contains('open');
      if(open) noteArea.value = readGlobalNote();
      setNoteOpen(open);
    });
    noteCollapse.addEventListener('click', ()=> setNoteOpen(false));

    // draggable note
    (function dragNote(){
      const head = document.getElementById('noteHead');
      let down=false, sx=0, sy=0, ox=0, oy=0;
      head.addEventListener('mousedown', (e)=>{
        down=true; sx=e.clientX; sy=e.clientY;
        const r = note.getBoundingClientRect();
        ox=r.left; oy=r.top;
      });
      window.addEventListener('mousemove', (e)=>{
        if(!down) return;
        const nx = ox + (e.clientX - sx);
        const ny = oy + (e.clientY - sy);
        note.style.left = Math.max(8, Math.min(window.innerWidth - 220, nx)) + 'px';
        note.style.top  = Math.max(46, Math.min(window.innerHeight - 80, ny)) + 'px';
        note.style.right = "auto";
      });
      window.addEventListener('mouseup', ()=> down=false);
    })();

    /* =========================================================
       INTENSIFIERS
    ========================================================= */
    const I = { VOL:.84, BLUR:.78, GLITCH:.82, SEAR:.70, NOISE:.68 };
    function clamp01(x){ return Math.max(0, Math.min(1, x)); }
    function setVar(name,val){
      I[name]=clamp01(val);
      document.documentElement.style.setProperty(`--I_${name}`, I[name]);
      const el = document.getElementById('v'+name);
      if(el) el.textContent = I[name].toFixed(2);
      persistSoon();
    }
    function bindRange(name, id){
      const r = document.getElementById(id);
      r.addEventListener('input', ()=> setVar(name, r.value/1000));
      setVar(name, r.value/1000);
    }
    bindRange('VOL','rVOL');
    bindRange('BLUR','rBLUR');
    bindRange('GLITCH','rGLITCH');
    bindRange('SEAR','rSEAR');
    bindRange('NOISE','rNOISE');

    const ctl = document.getElementById('ctl');
    document.getElementById('btnCtl').onclick = ()=> ctl.classList.toggle('hidden');
    document.getElementById('ctlCollapse').onclick = ()=> ctl.classList.add('hidden');

    // draggable ctl (optional, but keeps it from “getting stuck”)
    (function dragCtl(){
      const head = document.getElementById('ctlHead');
      let down=false, sx=0, sy=0, ox=0, oy=0;
      head.addEventListener('mousedown', (e)=>{
        down=true; sx=e.clientX; sy=e.clientY;
        const r = ctl.getBoundingClientRect();
        ox=r.left; oy=r.top;
        e.preventDefault();
      });
      window.addEventListener('mousemove', (e)=>{
        if(!down) return;
        const nx = ox + (e.clientX - sx);
        const ny = oy + (e.clientY - sy);
        ctl.style.left = Math.max(8, Math.min(window.innerWidth - 220, nx)) + 'px';
        ctl.style.top  = Math.max(46, Math.min(window.innerHeight - 80, ny)) + 'px';
        ctl.style.right = "auto";
      });
      window.addEventListener('mouseup', ()=> down=false);
    })();

    /* =========================================================
       SYSTEM UNIVERSALS (restricted)
       ESC: exit null
       N: toggle null
       SHIFT+I: invert
       SHIFT+B: home
       SHIFT+K: landing
       1–6: lights behind
    ========================================================= */
    const lights = document.getElementById('lights');
    const nullStatus = document.getElementById('nullStatus');
    const invStatus  = document.getElementById('invStatus');
    const lightStatus= document.getElementById('lightStatus');
    let nullMode=false, invert=false, lightMode=0;

    function setNull(on){
      nullMode=!!on;
      document.body.classList.toggle('null', nullMode);
      nullStatus.textContent = nullMode ? "ON" : "OFF";
      persistSoon();
    }
    function setInvert(on){
      invert=!!on;
      document.body.classList.toggle('invert', invert);
      invStatus.textContent = invert ? "ON" : "OFF";
      persistSoon();
    }
    function setLight(n){
      n = n|0; if(n<0) n=0; if(n>6) n=6;
      lightMode=n;
      lightStatus.textContent = String(n);
      lights.classList.toggle('on', n!==0);
      lights.style.animation="none";
      void lights.offsetHeight;
      if(n===0) { persistSoon(); return; }
      const map={
        1:"strobe1 1s linear infinite",
        2:"strobe2 1s linear infinite",
        3:"strobe3 1s linear infinite",
        4:"strobe4 1s linear infinite",
        5:"strobe5 1s linear infinite",
        6:"strobe6 1s linear infinite"
      };
      lights.style.animation = map[n] || map[1];
      persistSoon();
    }

    window.addEventListener('keydown', (e)=>{
      if(e.key==="Escape"){ setNull(false); return; }
      if(e.key.toLowerCase()==="n"){ setNull(!nullMode); return; }
      if(e.shiftKey && e.key.toLowerCase()==="i"){ setInvert(!invert); return; }
      if(e.shiftKey && e.key.toLowerCase()==="b"){ location.href=SHELL_HOME_URL; return; }
      if(e.shiftKey && e.key.toLowerCase()==="k"){ location.href=LANDING_URL; return; }
      if(!e.shiftKey && !e.ctrlKey && !e.metaKey){
        const n = Number(e.key);
        if([1,2,3,4,5,6].includes(n)) setLight(n);
      }
    });

    /* =========================================================
       COLLAPSE LOWERS (click-first)
    ========================================================= */
    function toggleBlock(btnId, bodyId){
      const btn = document.getElementById(btnId);
      const body = document.getElementById(bodyId);
      btn.onclick = ()=>{
        const hidden = body.style.display === "none";
        body.style.display = hidden ? "block" : "none";
      };
    }
    toggleBlock("stmtCollapse","stmtBody");
    toggleBlock("payCollapse","payBody");
    toggleBlock("ledgerCollapse","ledgerBody");

    /* =========================================================
       ENTROPIC GOAL (big center)
    ========================================================= */
    const goalEl = document.getElementById('goalNum');
    const goalText = document.getElementById('goalText');
    const s1 = document.getElementById('s1');
    const s2 = document.getElementById('s2');
    const s3 = document.getElementById('s3');

    function formatUSD(n){
      const s = Math.round(n).toString();
      return "$" + s.replace(/\B(?=(\d{3})+(?!\d))/g, ",");
    }
    let baseGoal = 12000000;

    function updateGoalEntropy(t){
      // 7+ digits always
      const v = I.VOL, g = I.GLITCH;

      const drift = (Math.sin(t*0.33) + Math.cos(t*0.21))*0.5;
      const chaos = (Math.sin(t*3.1) + Math.cos(t*3.9))*0.5 * v;
      const val = baseGoal * (1 + drift*0.07 + chaos*0.05);

      const txt = formatUSD(Math.max(1000000, val));
      goalText.textContent = txt;
      goalEl.setAttribute("data-goal", txt);

      // violent smear offsets
      const gdx  = (Math.sin(t*14.7)+Math.cos(t*11.1))*52 * g;
      const gdy  = (Math.cos(t*13.9)+Math.sin(t*9.8))*28 * g;
      const gdx2 = (Math.cos(t*16.4)+Math.sin(t*12.2))*88 * g;
      const gdy2 = (Math.sin(t*15.9)+Math.cos(t*10.6))*44 * g;

      goalEl.style.setProperty("--gdx", gdx.toFixed(2));
      goalEl.style.setProperty("--gdy", gdy.toFixed(2));
      goalEl.style.setProperty("--gdx2", gdx2.toFixed(2));
      goalEl.style.setProperty("--gdy2", gdy2.toFixed(2));

      // cut-up slices show goal fragments
      const frag = `<div style="font-weight:900;letter-spacing:.08em;font-size:clamp(44px,6.2vw,110px);filter:blur(${(I.BLUR*4).toFixed(2)}px);text-shadow:0 0 ${Math.round(I.SEAR*55)}px rgba(255,255,255,.6);opacity:.9;">${txt}</div>`;
      s1.innerHTML = frag; s2.innerHTML = frag; s3.innerHTML = frag;
    }

    /* =========================================================
       VOLATILE BAR (tearing, wobble)
    ========================================================= */
    const barFill = document.getElementById('barFill');
    const pctEl = document.getElementById('pct');
    const volEl = document.getElementById('vol');
    const t1 = document.getElementById('t1');
    const t2 = document.getElementById('t2');
    const t3 = document.getElementById('t3');
    let p = 0.33;

    function updateVolatileBar(t){
      const v = I.VOL, g = I.GLITCH;

      const drift = Math.sin(t*(0.28 + v*1.6))*0.03;
      const wave  = Math.sin(t*(2.1 + v*5.4))*0.10 + Math.cos(t*(1.2 + v*4.1))*0.08;
      const micro = (Math.sin(t*24.0) + Math.cos(t*18.0))*0.012 * v;

      const target = 0.45 + drift + wave + micro;

      p = p*0.90 + target*0.10;
      p += (Math.sin(t*12.1)*0.006 + Math.cos(t*9.7)*0.006) * v;

      const spike = (Math.sin(t*(4.2+v*6.0))*0.5+0.5);
      const spikeAmt = spike > 0.90 ? (spike-0.90)*3.0*v : 0;

      const pp = Math.max(0.02, Math.min(0.98, p + spikeAmt));

      const wobble = (Math.sin(t*7.2)+Math.cos(t*5.1))*3.0*v;
      const scaleY = 1 + (Math.sin(t*6.6)*0.22*v);
      const shear = (Math.sin(t*9.0)+Math.cos(t*7.4))*2.2*g;

      barFill.style.width = (pp*100).toFixed(2) + "%";
      barFill.style.transform = `skewX(${shear}deg) rotate(${wobble*0.15}deg) scaleY(${scaleY})`;

      const pct = Math.round(pp*100);
      pctEl.textContent = pct + "%";
      const vv = Math.round((v*100) + (Math.abs(wave)*520));
      volEl.textContent = vv.toString();

      // tears move
      const x1 = ((Math.sin(t*2.8)+1)/2) * 100;
      const x2 = ((Math.cos(t*3.3)+1)/2) * 100;
      const x3 = ((Math.sin(t*4.1)+Math.cos(t*2.2)+2)/4) * 100;
      t1.style.left = `calc(${x1}% - 90px)`;
      t2.style.left = `calc(${x2}% - 90px)`;
      t3.style.left = `calc(${x3}% - 90px)`;
      t1.style.transform = `skewX(${(Math.sin(t*6.2)*18*g).toFixed(2)}deg)`;
      t2.style.transform = `skewX(${(Math.cos(t*5.6)*22*g).toFixed(2)}deg)`;
      t3.style.transform = `skewX(${(Math.sin(t*7.1)*26*g).toFixed(2)}deg)`;
    }

    /* =========================================================
       CUT-UP MOTION FOR SLICES
    ========================================================= */
    function updateSlices(t){
      const g = I.GLITCH;
      const b = I.BLUR;

      // each slice offset (hard cut-up)
      const dx1 = (Math.sin(t*9.2)+Math.cos(t*6.6))*60*g;
      const dx2 = (Math.cos(t*8.7)+Math.sin(t*5.9))*80*g;
      const dx3 = (Math.sin(t*10.1)+Math.cos(t*7.4))*110*g;

      const dy1 = (Math.cos(t*7.3)+Math.sin(t*6.1))*10*g;
      const dy2 = (Math.sin(t*6.9)+Math.cos(t*5.4))*12*g;
      const dy3 = (Math.cos(t*8.1)+Math.sin(t*7.0))*14*g;

      s1.style.transform = `translate(${dx1.toFixed(2)}px, ${dy1.toFixed(2)}px)`;
      s2.style.transform = `translate(${dx2.toFixed(2)}px, ${dy2.toFixed(2)}px)`;
      s3.style.transform = `translate(${dx3.toFixed(2)}px, ${dy3.toFixed(2)}px)`;

      s1.style.filter = `blur(${(b*3.4).toFixed(2)}px)`;
      s2.style.filter = `blur(${(b*4.2).toFixed(2)}px)`;
      s3.style.filter = `blur(${(b*5.0).toFixed(2)}px)`;
    }

    /* =========================================================
       GLOBAL JITTER (stage)
    ========================================================= */
    function updateJitter(t){
      const g = I.GLITCH;
      // occasional hard pops
      const pop = (Math.sin(t*5.5)*0.5+0.5);
      const hard = pop > 0.985 ? 1 : 0;

      const jx = (Math.sin(t*13.0)+Math.cos(t*10.4))*1.6*(g*3.0) + hard*(Math.random()*24-12);
      const jy = (Math.cos(t*12.2)+Math.sin(t*9.8))*1.2*(g*2.6) + hard*(Math.random()*16-8);

      document.documentElement.style.setProperty("--jx", jx.toFixed(2));
      document.documentElement.style.setProperty("--jy", jy.toFixed(2));
    }

    /* =========================================================
       LEDGER (local)
    ========================================================= */
    const rowsEl = document.getElementById('rows');
    const ledgerCount = document.getElementById('ledgerCount');
    const tagEl = document.getElementById('tag');

    function loadLedger(){
      const raw = localStorage.getItem(LEDGER_KEY);
      if(!raw) return [];
      try{ const arr = JSON.parse(raw); return Array.isArray(arr)?arr:[]; }catch{ return []; }
    }
    function saveLedger(arr){
      localStorage.setItem(LEDGER_KEY, JSON.stringify(arr));
    }
    function escapeHtml(s){
      return String(s)
        .replaceAll("&","&amp;")
        .replaceAll("<","&lt;")
        .replaceAll(">","&gt;")
        .replaceAll('"',"&quot;")
        .replaceAll("'","&#039;");
    }
    function renderLedger(arr){
      rowsEl.innerHTML = "";
      for(const r of arr.slice().reverse()){
        const tr = document.createElement('tr');

        const tdT = document.createElement('td');
        tdT.textContent = r.ts || "";
        tr.appendChild(tdT);

        const tdTag = document.createElement('td');
        tdTag.innerHTML = `<span class="etch">${escapeHtml(r.tag||"")}</span>`;
        tr.appendChild(tdTag);

        const tdM = document.createElement('td');
        tdM.innerHTML = `<span class="pill">${escapeHtml(r.method||"—")}</span>`;
        tr.appendChild(tdM);

        const tdN = document.createElement('td');
        tdN.textContent = r.note || "";
        tr.appendChild(tdN);

        rowsEl.appendChild(tr);
      }
      ledgerCount.textContent = String(arr.length);
    }

    function etchTag(){
      const tag = (tagEl.value || "").trim();
      if(!tag) return;
      const arr = loadLedger();
      arr.push({
        ts: new Date().toISOString(),
        tag,
        method: "UNVERIFIED",
        note: "LOCAL ETCH (BACKEND CONFIRM LATER)"
      });
      saveLedger(arr);
      renderLedger(arr);
      tagEl.value = "";
      persistSoon();
    }
    document.getElementById('etch').onclick = etchTag;

    /* Copy buttons */
    function copyTextFrom(id){
      const el = document.getElementById(id);
      const txt = el.textContent.trim();
      navigator.clipboard?.writeText(txt).catch(()=>{});
    }
    document.getElementById('copyBTC').onclick = ()=> copyTextFrom('btcAddr');
    document.getElementById('copyETH').onclick = ()=> copyTextFrom('ethAddr');
    document.getElementById('copyOther').onclick = ()=> copyTextFrom('otherPay');

    /* =========================================================
       STATE IO (portable)
    ========================================================= */
    const statePanel = document.getElementById('statePanel');
    const stateText  = document.getElementById('stateText');
    document.getElementById('btnState').onclick = ()=>{
      statePanel.style.display = (statePanel.style.display==="none"||!statePanel.style.display) ? "block" : "none";
      if(statePanel.style.display==="block") stateText.value = exportState();
    };
    document.getElementById('stateClose').onclick = ()=> statePanel.style.display="none";
    function safeParseJSON(s){ try{return JSON.parse(s);}catch{return null;} }

    let persistSoonT=null;
    function persistSoon(){
      clearTimeout(persistSoonT);
      persistSoonT=setTimeout(()=>persistState(), 160);
    }

    function getRoomState(){
      return {
        I:{...I},
        lightMode, nullMode, invert,
        ctlHidden: ctl.classList.contains('hidden'),
        ctlPos: (()=>{ const r=ctl.getBoundingClientRect(); return {x:r.left,y:r.top}; })(),
        goalBase: baseGoal,
        btc: document.getElementById('btcAddr').textContent.trim(),
        eth: document.getElementById('ethAddr').textContent.trim(),
        other: document.getElementById('otherPay').textContent.trim(),
        stmtHidden: (document.getElementById('stmtBody').style.display==="none"),
        payHidden: (document.getElementById('payBody').style.display==="none"),
        ledgerHidden: (document.getElementById('ledgerBody').style.display==="none")
      };
    }
    function setRoomState(s){
      if(!s) return;
      if(s.I){
        for(const k of Object.keys(I)){
          if(typeof s.I[k] === "number") setVar(k, s.I[k]);
        }
      }
      if(typeof s.lightMode==="number") setLight(s.lightMode|0);
      if(typeof s.nullMode==="boolean") setNull(s.nullMode);
      if(typeof s.invert==="boolean") setInvert(s.invert);

      if(typeof s.ctlHidden==="boolean"){
        ctl.classList.toggle('hidden', s.ctlHidden);
      }
      if(s.ctlPos && typeof s.ctlPos.x==="number" && typeof s.ctlPos.y==="number"){
        ctl.style.left = Math.max(8, Math.min(window.innerWidth - 220, s.ctlPos.x)) + 'px';
        ctl.style.top  = Math.max(46, Math.min(window.innerHeight - 80, s.ctlPos.y)) + 'px';
        ctl.style.right = "auto";
      }
      if(typeof s.goalBase==="number") baseGoal = Math.max(1000000, s.goalBase);

      if(typeof s.btc==="string") document.getElementById('btcAddr').textContent = s.btc;
      if(typeof s.eth==="string") document.getElementById('ethAddr').textContent = s.eth;
      if(typeof s.other==="string") document.getElementById('otherPay').textContent = s.other;

      if(typeof s.stmtHidden==="boolean") document.getElementById('stmtBody').style.display = s.stmtHidden ? "none" : "block";
      if(typeof s.payHidden==="boolean") document.getElementById('payBody').style.display = s.payHidden ? "none" : "block";
      if(typeof s.ledgerHidden==="boolean") document.getElementById('ledgerBody').style.display = s.ledgerHidden ? "none" : "block";
    }

    function getEnvelope(){
      return {
        _ketadata:true,
        file_id: FILE_ID,
        room_id: ROOM_ID,
        version: VERSION,
        exported_at: new Date().toISOString(),
        state: getRoomState(),
        ledger: loadLedger()
      };
    }
    function exportState(){ return JSON.stringify(getEnvelope(), null, 2); }
    function exportStateToClipboard(){
      const s = exportState();
      navigator.clipboard?.writeText(s).catch(()=>{});
      return s;
    }
    function importStateFromText(text){
      const obj = safeParseJSON(text);
      if(!obj || !obj._ketadata) throw new Error("NOT_KETADATA_STATE");
      if(obj.state) setRoomState(obj.state);
      if(Array.isArray(obj.ledger)) { saveLedger(obj.ledger); renderLedger(obj.ledger); }
      persistState();
    }
    function persistState(){
      try{ localStorage.setItem(STATE_KEY, JSON.stringify(getEnvelope())); }catch{}
    }
    function loadPersistedState(){
      const raw = localStorage.getItem(STATE_KEY);
      if(!raw) return false;
      const obj = safeParseJSON(raw);
      if(!obj) return false;
      if(obj.state) setRoomState(obj.state);
      if(Array.isArray(obj.ledger)) { saveLedger(obj.ledger); renderLedger(obj.ledger); }
      return true;
    }

    document.getElementById('stateExport').onclick = ()=> { stateText.value = exportState(); };
    document.getElementById('stateCopy').onclick   = ()=> { stateText.value = exportStateToClipboard(); };
    document.getElementById('stateImport').onclick = ()=> { try{ importStateFromText(stateText.value); }catch{} };
    document.getElementById('stateSave').onclick   = ()=> { persistState(); };
    document.getElementById('stateLoad').onclick   = ()=> { loadPersistedState(); stateText.value = exportState(); };
    document.getElementById('stateClearLedger').onclick = ()=>{
      localStorage.removeItem(LEDGER_KEY);
      renderLedger([]);
      persistSoon();
    };

    /* =========================================================
       BOOT LEDGER + LOAD STATE
    ========================================================= */
    renderLedger(loadLedger());
    loadPersistedState();

    /* =========================================================
       TICK
    ========================================================= */
    function tick(){
      const t = performance.now()/1000;

      updateGoalEntropy(t);
      updateVolatileBar(t);
      updateSlices(t);
      updateJitter(t);

      requestAnimationFrame(tick);
    }
    tick();
  </script>

  <!-- ============================================================
  KETADATA HTML SERIALIZATION STAMP
  AE/EE/WB: WB
  FILE_ID: KETADATA_FUNDING_TERMINAL
  ROOM_ID: STORE
  VERSION: v1
  UPDATED_AT: 2025-12-25T00:00:00-05:00
  CHANGELOG:
  - REBUILT LAYOUT: BIG CENTER ENTROPIC GOAL + CUT-UP SLICES + VOLATILE TEARING BAR
  - FIXED TOPBAR/CONTROLLER DOCKING: CONTROLS NEVER OVERLAP TOPBAR
  - MORE DRAMATIC GLITCH/JITTER/TEARS DRIVEN BY INTENSIFIERS
  - PRESERVED STATE IO + GLOBAL KETA_NOTE ICON LAW + SYSTEM UNIVERSALS
  - NULL MODE HIDES CHROME/CONTROLLERS/LOWER PANELS ONLY (STAGE REMAINS)
  ============================================================ -->
</body>
</html>
